version: '3'
services:
  icecandy_localchain:
    build:
      context: ./docker
      dockerfile: ./docker/Dockerfile
    container_name: icecandy_localchain
    ports:
      - 8545:8545
    volumes:
      - ./:/usr/src/app
    command: yarn hardhat node
